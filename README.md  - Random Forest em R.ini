Predição de Churn com Random Forest em R
Este projeto tem como objetivo prever a evasão de clientes (Churn) utilizando a técnica de Random Forest em R. O processo inclui preparação dos dados, balanceamento da base, ajuste de hiperparâmetros e avaliação do modelo com métricas e visualizações.

📦 Requisitos
Antes de rodar o script, certifique-se de que os seguintes pacotes estejam instalados:

r
Copiar
Editar
install.packages(c("caret", "randomForest", "e1071", "ggplot2", "ROSE"))
▶️ Etapas do Script
1. Carregamento das Bibliotecas
Bibliotecas utilizadas para modelagem, visualização, pré-processamento e balanceamento.

2. Importação dos Dados
O usuário seleciona manualmente o arquivo .csv com os dados.

r
Copiar
Editar
caminho <- file.choose()
dados <- read.csv(caminho, stringsAsFactors = TRUE)
3. Limpeza dos Dados
Remove a coluna customerID

Converte valores vazios para NA

Remove linhas com valores faltantes

Converte Churn para fator binário (1 = Yes, 0 = No)

4. Divisão Treino/Teste
70% dos dados para treino

30% para teste

Utiliza createDataPartition para estratificação

5. Balanceamento dos Dados
Utiliza a técnica ROSE (Random OverSampling Examples) para corrigir desbalanceamento da variável alvo (Churn) na base de treino.

6. Ajuste de Hiperparâmetro mtry
Utiliza a função tuneRF para buscar o valor ideal do parâmetro mtry (número de variáveis selecionadas a cada split na Random Forest).

7. Treinamento do Modelo
Treinamento do modelo de Random Forest com 500 árvores e o melhor mtry encontrado.

8. Predição e Avaliação
Realiza predições no conjunto de teste

Exibe Matriz de Confusão com métricas de acurácia, sensibilidade e especificidade

Gera gráfico comparando acertos e erros nas predições

📊 Exemplo de Saída - Gráfico
O script gera um gráfico que compara as predições corretas e incorretas:

Eixo X: Churn Previsto (0 ou 1)

Cores: Correto ou Incorreto

Objetivo: Visualizar o desempenho do modelo de forma intuitiva

📁 Dados de Entrada
O arquivo .csv deve conter, no mínimo:

Uma variável binária chamada Churn (valores "Yes" ou "No")

Outras variáveis explicativas (demográficas, comportamentais, etc.)

A coluna customerID, se existir, será descartada

